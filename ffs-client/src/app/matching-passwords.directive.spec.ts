import { MatchingPasswordsDirective } from './matching-passwords.directive';

describe('MatchingPasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchingPasswordsDirective();
    expect(directive)
      .toBeTruthy();
  });
});
